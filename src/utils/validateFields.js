// Utility function to check if a field is a boolean
const isBoolean = (value) => typeof value === 'boolean';

// Utility function to check if a field is a number
const isNumber = (value) => typeof value === 'number';

module.exports = {
  isBoolean,
  isNumber,
};
